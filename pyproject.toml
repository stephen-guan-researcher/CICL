[project]
name = "cicl-agent"
version = "0.1.0"
description = "Decision-aware context selection and compression for tool-using LLM agents."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]
plots = ["matplotlib>=3.8"]
graph = ["networkx>=3.2"]
pdf = ["pypdf>=4.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
